{
  "openapi": "3.0.0",
  "info": {
    "title": "PulseFit Gym API",
    "version": "1.0.0",
    "description": "API Documentation for PulseFit Gym Management System"
  },
  "servers": [
    {
      "url": "http://localhost:5000/api",
      "description": "Development Server"
    }
  ],
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ],
  "paths": {

    "/auth/register": {
      "post": {
        "tags": ["Auth"],
        "summary": "Register a new user",
        "security": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "fullName": { "type": "string" },
                  "email": { "type": "string" },
                  "password": { "type": "string", "description": "Min 8 chars, 1 uppercase, 1 number, 1 special character" },
                  "phone": { "type": "string" }
                },
                "required": ["fullName", "email", "password"]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "User registered successfully" },
          "400": { "description": "Validation error or user already exists" }
        }
      }
    },

    "/auth/login": {
      "post": {
        "tags": ["Auth"],
        "summary": "Login user",
        "security": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "email": { "type": "string" },
                  "password": { "type": "string" }
                },
                "required": ["email", "password"]
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Login successful" },
          "400": { "description": "Invalid credentials" }
        }
      }
    },

    "/users/me": {
      "get": {
        "tags": ["Users"],
        "summary": "Get current user profile",
        "responses": {
          "200": { "description": "User profile data" },
          "401": { "description": "Not authenticated" }
        }
      },
      "put": {
        "tags": ["Users"],
        "summary": "Update current user profile",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "fullName": { "type": "string" },
                  "phone": { "type": "string" },
                  "password": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Profile updated" }
        }
      }
    },

    "/users": {
      "get": {
        "tags": ["Users"],
        "summary": "Get all users (Admin only)",
        "parameters": [
          {
            "name": "search",
            "in": "query",
            "description": "Search by name or email",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": { "description": "List of all users" },
          "403": { "description": "Admin access required" }
        }
      },
      "post": {
        "tags": ["Users"],
        "summary": "Create a new user (Admin only)",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "fullName": { "type": "string" },
                  "email": { "type": "string" },
                  "password": { "type": "string" },
                  "phone": { "type": "string" },
                  "role": { "type": "string", "enum": ["member", "admin"] },
                  "status": { "type": "string", "enum": ["active", "inactive"] }
                },
                "required": ["fullName", "email", "password"]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "User created" },
          "400": { "description": "Validation error or email already in use" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/users/{id}": {
      "get": {
        "tags": ["Users"],
        "summary": "Get a specific user by ID (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "User data" },
          "404": { "description": "User not found" },
          "403": { "description": "Admin access required" }
        }
      },
      "put": {
        "tags": ["Users"],
        "summary": "Update a user (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "fullName": { "type": "string" },
                  "phone": { "type": "string" },
                  "role": { "type": "string", "enum": ["member", "admin"] },
                  "status": { "type": "string", "enum": ["active", "inactive"] },
                  "password": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "User updated" },
          "404": { "description": "User not found" },
          "403": { "description": "Admin access required" }
        }
      },
      "delete": {
        "tags": ["Users"],
        "summary": "Delete a user (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "User deleted" },
          "400": { "description": "Cannot delete your own account" },
          "404": { "description": "User not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/classes": {
      "get": {
        "tags": ["Classes"],
        "summary": "Get all classes",
        "security": [],
        "responses": {
          "200": { "description": "List of all classes" }
        }
      },
      "post": {
        "tags": ["Classes"],
        "summary": "Create a new class (Admin only)",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "instructor": { "type": "string" },
                  "type": { "type": "string", "enum": ["HIIT", "Yoga", "Strength", "Cycling", "Mobility", "Boxing"] },
                  "scheduledAt": { "type": "string", "format": "date-time" },
                  "durationMinutes": { "type": "number" },
                  "capacity": { "type": "number" },
                  "intensity": { "type": "string", "enum": ["low", "moderate", "high"] }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Class created" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/classes/{id}": {
      "get": {
        "tags": ["Classes"],
        "summary": "Get a specific class by ID",
        "security": [],
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Class data" },
          "404": { "description": "Class not found" }
        }
      },
      "put": {
        "tags": ["Classes"],
        "summary": "Update a class (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Class updated" },
          "404": { "description": "Class not found" }
        }
      },
      "delete": {
        "tags": ["Classes"],
        "summary": "Delete a class (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Class deleted successfully" },
          "404": { "description": "Class not found" }
        }
      }
    },

    "/equipment": {
      "get": {
        "tags": ["Equipment"],
        "summary": "Get all equipment (Admin only)",
        "responses": {
          "200": { "description": "List of all equipment" },
          "403": { "description": "Admin access required" }
        }
      },
      "post": {
        "tags": ["Equipment"],
        "summary": "Create equipment (Admin only)",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "status": { "type": "string" },
                  "quantity": { "type": "number" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Equipment created" },
          "400": { "description": "Validation error" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/equipment/{id}": {
      "get": {
        "tags": ["Equipment"],
        "summary": "Get a specific equipment item (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Equipment data" },
          "404": { "description": "Equipment not found" },
          "403": { "description": "Admin access required" }
        }
      },
      "put": {
        "tags": ["Equipment"],
        "summary": "Update equipment (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "status": { "type": "string" },
                  "quantity": { "type": "number" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Equipment updated" },
          "404": { "description": "Equipment not found" },
          "403": { "description": "Admin access required" }
        }
      },
      "delete": {
        "tags": ["Equipment"],
        "summary": "Delete equipment (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Equipment deleted" },
          "404": { "description": "Equipment not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/bookings": {
      "post": {
        "tags": ["Bookings"],
        "summary": "Book a class",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "classId": { "type": "string" }
                },
                "required": ["classId"]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Booking successful" },
          "400": { "description": "Class full or already booked" }
        }
      },
      "get": {
        "tags": ["Bookings"],
        "summary": "Get all bookings (Admin only)",
        "responses": {
          "200": { "description": "List of all bookings" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/bookings/my": {
      "get": {
        "tags": ["Bookings"],
        "summary": "Get current user's bookings",
        "responses": {
          "200": { "description": "List of user bookings" }
        }
      }
    },

    "/bookings/{id}/cancel": {
      "patch": {
        "tags": ["Bookings"],
        "summary": "Cancel a booking",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Booking cancelled" },
          "400": { "description": "Booking already cancelled" },
          "404": { "description": "Booking not found" }
        }
      }
    },

    "/plans": {
      "get": {
        "tags": ["Plans"],
        "summary": "Get all active plans (pass ?all=true for all plans)",
        "security": [],
        "parameters": [
          { "name": "all", "in": "query", "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "List of plans" }
        }
      },
      "post": {
        "tags": ["Plans"],
        "summary": "Create a new plan (Admin only)",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "title": { "type": "string" },
                  "description": { "type": "string" },
                  "price": { "type": "number" },
                  "durationDays": { "type": "number" },
                  "category": { "type": "string", "enum": ["Drop-in", "Monthly", "Annual"] },
                  "features": { "type": "array", "items": { "type": "string" } },
                  "isActive": { "type": "boolean" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Plan created" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/plans/{id}": {
      "get": {
        "tags": ["Plans"],
        "summary": "Get a specific plan by ID",
        "security": [],
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Plan data" },
          "404": { "description": "Plan not found" }
        }
      },
      "put": {
        "tags": ["Plans"],
        "summary": "Update a plan (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "title": { "type": "string" },
                  "description": { "type": "string" },
                  "price": { "type": "number" },
                  "durationDays": { "type": "number" },
                  "category": { "type": "string", "enum": ["Drop-in", "Monthly", "Annual"] },
                  "features": { "type": "array", "items": { "type": "string" } },
                  "isActive": { "type": "boolean" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Plan updated" },
          "404": { "description": "Plan not found" },
          "403": { "description": "Admin access required" }
        }
      },
      "delete": {
        "tags": ["Plans"],
        "summary": "Delete a plan (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Plan deleted" },
          "404": { "description": "Plan not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/subscriptions": {
      "post": {
        "tags": ["Subscriptions"],
        "summary": "Subscribe to a plan",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "planId": { "type": "string" }
                },
                "required": ["planId"]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Subscription successful" },
          "400": { "description": "Already have an active plan" }
        }
      },
      "get": {
        "tags": ["Subscriptions"],
        "summary": "Get all subscriptions (Admin only)",
        "responses": {
          "200": { "description": "List of all subscriptions" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/subscriptions/my": {
      "get": {
        "tags": ["Subscriptions"],
        "summary": "Get current user's active subscription",
        "responses": {
          "200": { "description": "Active subscription details or null" }
        }
      }
    },

    "/subscriptions/{id}/cancel": {
      "patch": {
        "tags": ["Subscriptions"],
        "summary": "Cancel a subscription",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Subscription cancelled" },
          "400": { "description": "Subscription is not active" },
          "404": { "description": "Subscription not found" }
        }
      }
    },

    "/products": {
      "get": {
        "tags": ["Products"],
        "summary": "Get all products (pass ?all=true to include inactive)",
        "security": [],
        "parameters": [
          { "name": "all", "in": "query", "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "List of products" }
        }
      },
      "post": {
        "tags": ["Products"],
        "summary": "Create a product (Admin only)",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "price": { "type": "number" },
                  "stock": { "type": "number" },
                  "category": { "type": "string" },
                  "imageUrl": { "type": "string" },
                  "isActive": { "type": "boolean" }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Product created" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/products/{id}": {
      "get": {
        "tags": ["Products"],
        "summary": "Get a specific product by ID",
        "security": [],
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Product data" },
          "404": { "description": "Product not found" }
        }
      },
      "put": {
        "tags": ["Products"],
        "summary": "Update a product (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "name": { "type": "string" },
                  "description": { "type": "string" },
                  "price": { "type": "number" },
                  "stock": { "type": "number" },
                  "category": { "type": "string" },
                  "imageUrl": { "type": "string" },
                  "isActive": { "type": "boolean" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Product updated" },
          "404": { "description": "Product not found" },
          "403": { "description": "Admin access required" }
        }
      },
      "delete": {
        "tags": ["Products"],
        "summary": "Delete a product (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Product deleted" },
          "404": { "description": "Product not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/products/{id}/buy": {
      "patch": {
        "tags": ["Products"],
        "summary": "Reserve a product (buy)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Product reserved successfully" },
          "400": { "description": "Out of stock" },
          "404": { "description": "Product not found" }
        }
      }
    },

    "/reservations/my": {
      "get": {
        "tags": ["Reservations"],
        "summary": "Get current user's product reservations",
        "responses": {
          "200": { "description": "List of user's reservations" }
        }
      }
    },

    "/reservations": {
      "get": {
        "tags": ["Reservations"],
        "summary": "Get all product reservations (Admin only)",
        "parameters": [
          {
            "name": "status",
            "in": "query",
            "description": "Filter by status",
            "schema": {
              "type": "string",
              "enum": ["all", "reserved", "picked_up", "cancelled"]
            }
          }
        ],
        "responses": {
          "200": { "description": "List of all reservations" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/reservations/{id}/status": {
      "patch": {
        "tags": ["Reservations"],
        "summary": "Update reservation status (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "status": {
                    "type": "string",
                    "enum": ["reserved", "picked_up", "cancelled"]
                  }
                },
                "required": ["status"]
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Reservation status updated" },
          "400": { "description": "Invalid status" },
          "404": { "description": "Reservation not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/reservations/{id}/cancel": {
      "patch": {
        "tags": ["Reservations"],
        "summary": "Cancel own reservation",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Reservation cancelled" },
          "400": { "description": "Only active reservations can be cancelled" },
          "403": { "description": "Not authorized" },
          "404": { "description": "Reservation not found" }
        }
      }
    },

    "/feedback": {
      "post": {
        "tags": ["Feedback"],
        "summary": "Submit feedback",
        "security": [],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "properties": {
                  "rating": { "type": "number", "minimum": 1, "maximum": 5 },
                  "memberName": { "type": "string" },
                  "message": { "type": "string" }
                },
                "required": ["rating", "message"]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Feedback submitted" },
          "400": { "description": "Validation error" }
        }
      },
      "get": {
        "tags": ["Feedback"],
        "summary": "Get all feedback (Admin only)",
        "responses": {
          "200": { "description": "List of all feedback" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/feedback/{id}/resolve": {
      "patch": {
        "tags": ["Feedback"],
        "summary": "Mark feedback as resolved (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Feedback marked as resolved" },
          "404": { "description": "Feedback not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/feedback/{id}/reopen": {
      "patch": {
        "tags": ["Feedback"],
        "summary": "Reopen feedback (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Feedback reopened" },
          "404": { "description": "Feedback not found" },
          "403": { "description": "Admin access required" }
        }
      }
    },

    "/feedback/{id}": {
      "delete": {
        "tags": ["Feedback"],
        "summary": "Delete feedback (Admin only)",
        "parameters": [
          { "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }
        ],
        "responses": {
          "200": { "description": "Feedback deleted" },
          "404": { "description": "Feedback not found" },
          "403": { "description": "Admin access required" }
        }
      }
    }

  }
}